module.exports = {
  client: 'postgresql',
  connection: {
    database: '<%= @dbname %>',
    host: '<%= @host %>',
    port: <%= @port %>,
    user: '<%= @username %>',
    password: '<%= @password %>'
  },
  pool: {
    min: 2,
    max: 10
  },
  migrations: {
    tableName: 'knex_migrations'
  }
}
